lucky_numbers=[1,8,5,23,45]
friends=["ABC","SDE","ASR","SFS","LMN"]
#friends.extend(lucky_numbers)  --extends the ist by adding another list
#friends.append("EDS")    --appends or adds another item in the list
#friends.insert(1, "EEE")  --inserts new item on the position required and pushes next items ahead
#friends.remove("ABC")    --removes the item from the list
#friends.pop()      --deletes the last item of the list
#print(friends.index("SDE")) --prints the index
#lucky_numbers.sort() -- sorts the list in ascending order
#lucky_numbers.reverse() --reverses the order of given list
#friends=friends.copy() --copies one list in another
